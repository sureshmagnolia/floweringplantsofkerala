import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sureshmagnolia.keralaplants',
  appName: 'KERALA PLANTS',
  webDir: 'out'
};

export default config;
